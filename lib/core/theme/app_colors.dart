import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const Color primary = Color(0xFF1A3A6B);      // Deep FIFA blue
  static const Color secondary = Color(0xFFD4AF37);    // Gold
  static const Color accent = Color(0xFF00C853);       // Live green

  // Backgrounds
  static const Color background = Color(0xFF0F1923);
  static const Color surface = Color(0xFF1C2B3A);
  static const Color surfaceVariant = Color(0xFF243447);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8FA3B1);
  static const Color textDisabled = Color(0xFF4A5568);

  // Status
  static const Color success = Color(0xFF00C853);
  static const Color warning = Color(0xFFFFB300);
  static const Color error = Color(0xFFE53935);
  static const Color live = Color(0xFFFF1744);

  // Points
  static const Color positivePoints = Color(0xFF00E676);
  static const Color negativePoints = Color(0xFFFF5252);
  static const Color neutralPoints = Color(0xFF8FA3B1);

  // Cards
  static const Color yellowCard = Color(0xFFFFD600);
  static const Color redCard = Color(0xFFD32F2F);

  // Positions
  static const Color gkColor = Color(0xFFFF8F00);
  static const Color defColor = Color(0xFF1565C0);
  static const Color midColor = Color(0xFF2E7D32);
  static const Color fwdColor = Color(0xFFC62828);
}
